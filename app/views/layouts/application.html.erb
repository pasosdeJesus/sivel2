<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=Edge,chrome=1">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "Sivel 2.0a1 - Honduras" %></title>
    <%= csrf_meta_tags %>

    <!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
    <!--[if lt IE 9]>
      <script src="//cdnjs.cloudflare.com/ajax/libs/html5shiv/3.6.1/html5shiv.js" type="text/javascript"></script>
    <![endif]-->

    <%= stylesheet_link_tag "application", :media => "all" %>

    <!-- For third-generation iPad with high-resolution Retina display: -->
    <!-- Size should be 144 x 144 pixels -->
    <%= favicon_link_tag 'apple-touch-icon-144x144-precomposed.png', :rel => 'apple-touch-icon-precomposed', :type => 'image/png', :sizes => '144x144' %>

    <!-- For iPhone with high-resolution Retina display: -->
    <!-- Size should be 114 x 114 pixels -->
    <%= favicon_link_tag 'apple-touch-icon-114x114-precomposed.png', :rel => 'apple-touch-icon-precomposed', :type => 'image/png', :sizes => '114x114' %>

    <!-- For first- and second-generation iPad: -->
    <!-- Size should be 72 x 72 pixels -->
    <%= favicon_link_tag 'apple-touch-icon-72x72-precomposed.png', :rel => 'apple-touch-icon-precomposed', :type => 'image/png', :sizes => '72x72' %>

    <!-- For non-Retina iPhone, iPod Touch, and Android 2.1+ devices: -->
    <!-- Size should be 57 x 57 pixels -->
    <%= favicon_link_tag 'apple-touch-icon-precomposed.png', :rel => 'apple-touch-icon-precomposed', :type => 'image/png' %>

    <!-- For all other devices -->
    <!-- Size should be 32 x 32 pixels -->
    <%= favicon_link_tag 'favicon.ico', :rel => 'shortcut icon' %>

    <%= javascript_include_tag "application" %>
  </head>
  <body>

    <%= nav_bar :fixed => :top, :brand => "SIVeL 2.0a1 - Honduras", :responsive => true do %>
      <%= menu_group do %>
        <% if !current_usuario.nil? %>
          <%= menu_item "Casos", casos_path %>
          <%= menu_item "Actividades", actividades_path %>
        <% end %>
      <% end %>
      <%= menu_group :pull => :right do %>
        <%= menu_item "Documentacion", "/doc/html/index.html"%>
        <% if !current_usuario.nil? %>
					<%= drop_down "Administrar" do %>
            <%= menu_item "Clave", editar_registro_usuario_path %>
						<% if current_usuario.rol == Ability::ROLADMIN || 
							current_usuario.rol == Ability::ROLCOOR ||
							current_usuario.rol == Ability::ROLDIR %>
							<%= menu_item "Usuarios", usuarios_path %>
						<% end %>
						<% if current_usuario.rol == Ability::ROLADMIN || 
							current_usuario.rol == Ability::ROLDIR %>
              <%= menu_item "Tablas Básicas", tablasbasicas_path %>
              <!-- %= #menu_item "Casos", admin_casos_path % -->
				    <% end %>
          <% end %>
					<%= menu_item "Salir #{current_usuario.nusuario}", sign_out_path %>
        <% else %>
          <%= menu_item "Acerca de", acercade_path %>
					<%= menu_item "Iniciar Sesión", new_usuario_session_path %> 
        <% end %>
      <% end %>
    <% end %>

    <div class="container master-container">
	    <ul class="breadcrumb">
		    <%= yield :breadcrumb %>
	    </ul>
	    <%= bootstrap_flash %>
	    <%= yield %>
    </div>

    <footer align='right'>
    <p><span class='derechos'>Dominio Público de acuerdo a Legislación Colombiana<br/>
      Desarrollado por <a href="http://www.pasosdeJesus.org" target="_blank">Pasos de Jesús</a>. 2014. 
    </span></p>
    </footer>

    </div> <!-- /container -->

  </body>
</html>
